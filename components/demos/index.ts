import type { ComponentType } from 'react';
import { SpringTargetDemo } from './SpringTargetDemo';

// Add interactive demos here for each article

export const demoRegistry: Record<string, ComponentType> = {
  'spring-target-demo': SpringTargetDemo,
};
